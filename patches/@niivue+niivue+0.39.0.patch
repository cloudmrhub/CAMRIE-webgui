diff --git a/node_modules/@niivue/niivue/dist/index.js b/node_modules/@niivue/niivue/dist/index.js
index 5f99518..a0c80c2 100644
--- a/node_modules/@niivue/niivue/dist/index.js
+++ b/node_modules/@niivue/niivue/dist/index.js
@@ -127404,11 +127404,11 @@ var NVImage = class _NVImage {
           const val = u322[i2];
           u322[i2] = (val & 255) << 24 | (val & 65280) << 8 | val >> 8 & 65280 | val >> 24 & 255;
         }
-      } else if (this.hdr.numBitsPerVoxel === 64) {
-        const numBytesPerVoxel = this.hdr.numBitsPerVoxel / 8;
+      } else if (this.hdr.numBitsPerVoxel === 64 || this.hdr.numBitsPerVoxel === 128) {
+        const numBytesPerElement = 8;
         const u82 = new Uint8Array(imgRaw);
-        for (let index = 0; index < u82.length; index += numBytesPerVoxel) {
-          let offset = numBytesPerVoxel - 1;
+        for (let index = 0; index < u82.length; index += numBytesPerElement) {
+          let offset = numBytesPerElement - 1;
           for (let x = 0; x < offset; x++) {
             const theByte = u82[index + x];
             u82[index + x] = u82[index + offset];
@@ -127494,6 +127494,18 @@ var NVImage = class _NVImage {
         this.hdr.datatypeCode = this.DT_FLOAT;
         break;
       }
+      case this.DT_COMPLEX128: {
+        const f64 = new Float64Array(imgRaw);
+        const nvx2 = Math.floor(f64.length / 2);
+        this.imaginary = new Float32Array(nvx2);
+        this.img = new Float32Array(nvx2);
+        for (let i2 = 0; i2 < nvx2; i2++) {
+          this.img[i2] = f64[i2 * 2];
+          this.imaginary[i2] = f64[i2 * 2 + 1];
+        }
+        this.hdr.datatypeCode = this.DT_FLOAT;
+        break;
+      }
       default:
         throw new Error("datatype " + this.hdr.datatypeCode + " not supported");
     }
diff --git a/node_modules/@niivue/niivue/src/nvimage/index.ts b/node_modules/@niivue/niivue/src/nvimage/index.ts
index e2772c3..1165ab8 100644
--- a/node_modules/@niivue/niivue/src/nvimage/index.ts
+++ b/node_modules/@niivue/niivue/src/nvimage/index.ts
@@ -420,12 +420,13 @@ export class NVImage {
           const val = u32[i]
           u32[i] = ((val & 0xff) << 24) | ((val & 0xff00) << 8) | ((val >> 8) & 0xff00) | ((val >> 24) & 0xff)
         }
-      } else if (this.hdr.numBitsPerVoxel === 64) {
+      } else if (this.hdr.numBitsPerVoxel === 64 || this.hdr.numBitsPerVoxel === 128) {
         // inspired by MIT licensed code: https://github.com/rochars/endianness
-        const numBytesPerVoxel = this.hdr.numBitsPerVoxel / 8
+        // 64-bit or complex128 (2×64): swap each 8-byte element
+        const numBytesPerElement = 8
         const u8 = new Uint8Array(imgRaw)
-        for (let index = 0; index < u8.length; index += numBytesPerVoxel) {
-          let offset = numBytesPerVoxel - 1
+        for (let index = 0; index < u8.length; index += numBytesPerElement) {
+          let offset = numBytesPerElement - 1
           for (let x = 0; x < offset; x++) {
             const theByte = u8[index + x]
             u8[index + x] = u8[index + offset]
@@ -433,7 +434,7 @@ export class NVImage {
             offset--
           }
         }
-      } // if 64-bits
+      } // if 64-bits or 128-bits
     } // swap byte order
     switch (this.hdr.datatypeCode) {
       case this.DT_UNSIGNED_CHAR:
@@ -512,6 +513,20 @@ export class NVImage {
         this.hdr.datatypeCode = this.DT_FLOAT
         break
       }
+      case this.DT_COMPLEX128: {
+        // complex128 (float64 pairs) -> convert to complex64 (float32) for display only.
+        // Original imgRaw is read-only; we create temporary Float32 copies for rendering.
+        const f64 = new Float64Array(imgRaw)
+        const nvx = Math.floor(f64.length / 2)
+        this.imaginary = new Float32Array(nvx)
+        this.img = new Float32Array(nvx)
+        for (let i = 0; i < nvx; i++) {
+          this.img[i] = f64[i * 2]
+          this.imaginary[i] = f64[i * 2 + 1]
+        }
+        this.hdr.datatypeCode = this.DT_FLOAT
+        break
+      }
       default:
         throw new Error('datatype ' + this.hdr.datatypeCode + ' not supported')
     }
